import * as bdb from '../bdb/bdb'

// create token
// this will be a CREATE transaction with token name, token amount
// add outputs to this transaction for the TCR owner

export async function createToken(organizationId, token, outputs) {

}

export async function transferTokens(fromUser, toUser, token) {

}